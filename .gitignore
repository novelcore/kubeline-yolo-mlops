# Validated config artifacts (local runs)
.tmp/*.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/

# Environment files
.env

# pytest
.pytest_cache/
.coverage
htmlcov/

artifacts/

# Local MLflow tracking DB — never commit (contains run/experiment metadata)
mlflow.db
*.db

# ML local run artifacts — never commit (esp. to a public repo)
mlruns/
runs/
datasets/
*.npy
*.npz
*.cache
yolo11n-pose.pt
step_zero_spike.py

# OS cruft
.DS_Store

# Local dev stack (AC-06 verification — not for commit)
dev/
pipeline_config.ac06.yaml
